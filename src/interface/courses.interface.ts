export interface course {
    id: string;
    name: string;
    description: string;
    unit: string;
    photoUrl: string;
    supportMaterial: string;
    workSheetUrl: string;
    isActive: boolean;
    createdAt: number;
    updatedAt: number;
}